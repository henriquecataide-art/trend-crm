import { defineConfig } from 'vite';

// O deploy oficial usa Next.js/Vercel. Esta configuração neutra evita que
// ferramentas auxiliares procurem arquivos locais que não fazem parte do deploy.
export default defineConfig({});
